# Unicode scalar length

LexLean-generated string, UTF-8 byte, record-field and decoded-input length
operations. The receipt binds the unchanged source and complete generated tree.
Native and Wasm tests distinguish scalars from bytes and grapheme clusters.

/target
**/*.rs.bk
.result
lean/.lake
rust/target
rust/prod-core/kernel.ir
roots.json
coverage.md
goldens.txt
.direnv/
rust/prod-core/goldens.ir
/subset.json
output/

# Local skills-marketplace lockfile. Machine-specific paths and auth tokens;
# never belongs in the repo. Swept in once by a `git add -A`.
skills.lock
